import MainWrapper from "../wrappers/MainWrapper.tsx";

const Patterns = () => {
    return (
        <MainWrapper>
            <div>

            </div>
        </MainWrapper>
    );
};

export default Patterns;